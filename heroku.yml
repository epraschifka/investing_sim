build:
  docker:
    web: Dockerfile
run:
  web: gunicorn my_project.wsgi:application --bind 0.0.0.0:$PORT
  clock:
    command:
      - python clock.py
    image: python:3.8 # Replace with the appropriate base image for your clock process
